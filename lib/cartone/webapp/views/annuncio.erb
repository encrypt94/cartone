<a href="<%= annuncio['link']%>"><h1 class="page-header"><%= annuncio['title'] %></h1></a>
<p>
<%= annuncio['description']%>
</p>

<table class="table table-bordered table-striped">
  <colgroup>
    <col class="col-xs-1">
    <col class="col-xs-7">
  </colgroup>
  <tbody>
    <% annuncio['data'].each do |key,value|%>
    <tr>
      <td>
        <strong><%= key.capitalize %></strong>
      </td>
      <td>
	<%= value %>
	<% if key == "price"%>
	 €
	<% end %>
	<% if key == "size"%>
	 mq
	<% end %>
      </td>
    </tr>
    <% end %>
  <tbody>
</table>

<% annuncio['images'].each do |image| %>
<div class="col-lg-3 col-md-4 col-xs-6 thumb">
  <a class="thumbnail" href="<%= image %>">
    <img class="img-responsive" src="<%= image %>" alt="">
  </a>
</div>
<% end %>
